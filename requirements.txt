# Computational Wave Field Theory — code base. CPU-only; no GPU or network needed.
# Versions the committed records were produced with: Python 3.10.12, NumPy 2.2.6,
# SciPy 1.15.3, Matplotlib 3.10.3, stim 1.16.0, networkx 3.4.2.
numpy>=1.24
scipy>=1.10
matplotlib>=3.6
stim>=1.12          # stabilizer / Clifford experiments (cwf_a3_*, cwf_hp_*, cwf_t21_*, ...)
networkx>=3.0       # perfect-tensor / HaPPY-code experiments (cwf_a3b2_happy_perfect.py, ...)
mpmath>=1.3         # the number-theoretic playground scripts only (cwf_primon_*, cwf_prime_playground)
# Optional — only the four transformer scripts (cwf_complex_attention*.py, cwf_r5_phase3a.py):
# torch>=2.0
